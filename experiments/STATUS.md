# Experiment Suite — Live Status

Legend: ⬜ pre-registered · 🟨 implemented · 🟩 run · ✅ verdict issued

| # | Experiment | Paper | State | Verdict so far |
|---|---|---|---|---|
| **Shared** | `shared_lib` + self-tests | 1 & 3 | 🟩 | Self-tests reproduce K=1/4, Girsanov identifiability (HAC), the geometric jump guardrail, exact Hodge split. Adds `manifold_trajectory.py` (SPD trajectory sim + Cartan anti-development). Two real bugs found & fixed en route: HAC drift-test calibration, and a future-leakage bug in the predictability covariate. Extended for **BaseMetric**: `spd_manifold.py` now also carries the **log-Euclidean** (flat) and **Bures-Wasserstein** metrics, `anti_develop` gains a path-wise log-Euclidean branch, and `anti_develop_base` gives a base-point linearization for all four metrics; a self-test checks their defining identities (self-log zero, BW geodesic reaches its endpoint, (AB)^½ squares to AB, flat base==path). |
| **D** | drift_jump_confusion_sweep | 3 | ✅ | **CALIBRATIONAL for strong jumps, STRUCTURAL in one corner.** Only the strongest jump (collapse 0.1) stays cleanly separable from drift at every drift strength (AUC ≥ 0.93); as the jump weakens it collapses into drift over a structural region (AUC → 0.61 at collapse 0.7, drift 0.2) — no threshold separates; a hybrid metric is indicated there. Longer windows worsen it (AUC 0.99→0.76 as T 200→800, holonomy accumulation). Stable (0.41 ± 0.03 across seed banks). Metric-tail contrast NOT reproduced at this diffusion scale (excess kurtosis sqrt≈0.0 vs AIRM≈0.4, not the appendix's ≈4.5 vs ≈20) — which is why AIRM is not jump-blind here. Seeds re-spaced during review; conclusion unchanged. See `_results/README.md`. |
| **A** | localization_multiscale | 3 | ✅ | **Criterion met (2/15→15/15), but the fix is WINDOW SIZE, not multiscale.** Synthetic-adversarial (PhysioNet unreachable *at the time of that run*; access later established). Short window 2/15; a single large window already 15/15, matching the multiscale bank → the 5/15 failure is a window-size artifact. Drift guardrail holds. Caveats: offline symmetric windows. **Now tested on real EEG** by **ABC-Real**, which ran the scale comparison A1 skipped: window size does drive it (large 7/16 vs best short 4/16) and the multiscale bank adds nothing (6/16) — A's claim replicates in DIRECTION. But the MAGNITUDE does not: synthetic 2/15→15/15 becomes 4/16→7/16 on real recordings. An earlier version of this row said A1 had settled this; it had not — A1 used one fixed large window and never compared scales. |
| **B** | localization_priors | 3 | ✅ | **NO BENEFIT (mechanistic).** Causal smoothing of the predictability covariate monotonically degrades localization (0.36→0.07). An abrupt jump and a sharp excursion share a single-sample covariate signature, so smoothing blurs the jump faster than it denoises. The discriminator that works is persistence (window-mean, Exp A), not covariate smoothing. |
| **C** | cross_dataset | 3 | ✅ | **METHOD ROBUST via persistence.** A large persistence-sensitive window localizes robustly across paradigm strength (15/15 at every ratio) and fluctuation persistence (mild 15→13 as bursts approach window length). A+B+C synthesis: the 5/15 is a fragile-pointwise artifact fixed by a large window (not multiscale, not covariate smoothing); residual open problem = spontaneous bursts longer than the window. All synthetic. **C itself remains untested on real data** — its content is robustness across paradigm strength and fluctuation persistence, which needs corpora varying along those axes, not a second analysis of one corpus. (An earlier version of this row claimed real-EEG confirmation was no longer outstanding; that conflated C with A1/A3/Bench, which test different things.) The residual open problem — bursts longer than the window — stands. |
| **G** | poincare_recurrence_check | 1 | ✅ | **CONFIRMED** — recurrence fraction ≥ 0.95 for ε ≥ 0.05·span (0.978→0.995); drops to 0.51 at ε=0.01·span. Text's "≈1" holds but should state the ε regime. |
| **F** | tracking_cost_curve | 1 | ✅ | **CLAIM CONFIRMED.** Double-well explorer under tracking gain: D_ag falls 0.85→0.012 and λ∥ drops −0.09→−11.5, both monotone and graded (the k=1 bifurcation is washed out by exploratory noise). Sec 7.3's graded-monotone finite-gain agency cost is numerically vindicated. |
| **E** | rl_agents_trichotomy | 1 | ✅ | **TRICHOTOMY HOLDS — defence survives.** No candidate is the forbidden positive-entropy-plus-no-recurrence object: gradient→Case 1, hamiltonian→Case 3 (λ≈0, 100% rotational), curiosity→Case 1, novelty search→bounded-recurrent (R=1.0), Lorenz chaos→positive entropy (λ=+0.88) but recurrent (0.995). Sustained novelty without return needs a non-compact space (the escape horn), as the theorem predicts. Sec 7.5/7.6 vindicated. NB an end-to-end review found the recurrence test using **Euclidean** distance on coordinates wrapped to the torus (missing every recurrence across the wrap boundary, unlike **E2** which used the toroidal metric correctly); fixed to a toroidal k-d tree with a circular-mean reference scale. The fix runs in the direction that *favours* the paper and was applied on the outcome-independent ground that the metric was wrong: gradient 0.9976→0.9994, all others unchanged, Lorenz correctly untouched, **every classification identical**. See METHODOLOGY.md. |
| **H** | dissociation_power_analysis | 2 | ✅ | **MARGINAL feasibility; matching is a validity issue.** Imperfect S^{I+}/S^{I−} matching inflates the false-positive rate 3–7× (0.05→0.16→0.35) independent of sample size — the interaction contrast is confounded unless matching is tight (sd≲0.3σ). Even then a moderate effect (SNR 0.8) needs n≈40/condition. Executable but demands tight matching + large samples — a practical limitation, not just ethical. |
| **I** | criticality_sweep | 2 | ✅ | **CONFOUND ROBUST.** A genuinely critical generator (susceptibility exponent p>1) reproduces the gating differential in 15/24 cells (62%), robustly at p≥1.5 across noise/feedback, matching/exceeding the gated signal as criticality grows. Non-critical control (p=1) gives D≈0 (statistic valid). Sec 15.5's concession stands — CBRA's defensible arm is eliminative, not detection. |
| **J** | metabolic_null_resolution | 2 | ✅ | **RESOLUTION THRESHOLD QUANTIFIED.** The null absorbs the structured boundary residual only when its resolution is finer than h*≈0.7–0.9 ell, and h* grows monotonically with the diffusion length (0.73→0.93 as L_D 0.05→0.25·ell). Strong null := resolution < h*(L_D). Metabolism keeps the residual 20× more structured. Sec 7.2's qualitative claim is now a number. |

| **I2** | dissociation_confound | 2 | ✅ | **GROWING CONFOUND NEAR TRUE CRITICALITY.** Extends I to the dissociation test (M_diss, Sec 14.1). A bare critical generator with zero identity mechanism reaches 73% of the identity-linked reference D at the strongest cell (σ=0.97, low noise), rising monotonically with criticality — mirrors I's pattern (safe away from criticality, confounded near it). Sec 14.1 needs a sub-criticality control, like Sec 15.5 already implies for gating. → tested in **SubCrit**. |

| **SubCrit** | subcriticality_control | 2 | ✅ | **CONTROL WORKS — BUT ONLY WITH A SUBSAMPLING-ROBUST ESTIMATOR; §14.1 must name one.** Tests whether I2's proposed §14.1 control (require *measured* sub-criticality) actually recovers M_diss's power. Concept is sound (oracle drops the confound from 70% worst to 27% of reference) and with cleanly separated avalanches a standard estimator attains 24% with perfect discrimination. But on the physiological observable — a short continuous *subsampled* stream — the **naive** branching-ratio slope is attenuated toward zero (true σ=0.94→σ̂=0.79), false-certifying 79% of confounded near-critical systems as safe (residual 35%; the known Wilting-Priesemann subsampling bias). The subsampling-robust **MR** estimator removes the attenuation (σ_MR=0.92) and restores the control, but only marginally (29%, at the bar; 83% of confounded rejected). Honest close: the control is executable and recovers power **provided** distance-to-criticality is measured with a subsampling-robust estimator (MR-type) or clean event segmentation — not a naive one. That estimator requirement is the added §14.1 condition; with it, the loop closes. |

| **AD** | causal_vs_offline_localization | 3 | ✅ | **CAUSAL RECOVERABLE, AT A REPORTING-LAG COST.** Reconciles A (symmetric large window) and D (longer windows worsen drift/jump). Naive causal design fails (8/15); the standard adjacent-windows causal design is mathematically identical to symmetric shifted by w samples, and matches offline exactly (15/15) once lag-corrected — but pays a real ≥10-sample reporting lag. A/D's tension did NOT reproduce here: they are independent sub-problems, not a shared-window conflict. |

| **E2** | high_dim_trichotomy | 1 | ✅ | **HOLDS for the smooth resolved cells; hardest regime OPEN.** d=6 value-base-mutating agent; rebuilt integrator (tanh saturation + RK4 + vectorised), classified by resolution scaling. 4/12 converge (Case-1, λ≈−5 to −23) — no falsifier. 8/12 strong-recession cells are resolution-divergent (λ=121→217→528→722 as dt→DT/8, ~6×). **Presumptively** a 1/dt artifact — but λ·dt=[2.43,2.17,2.64,1.81] is noisy/non-monotonic (not a clean constant), and the near-discontinuity could be genuine abrupt value-base mutation (for which the smooth-flow recurrence premise fails), so it stays **numerically unresolved / genuinely open**, not conclusively artefactual. The one low-recurrence cell that could be a falsifier is one of these — undecided. §7.6 strengthened for smooth agents only. → **resolved in E2-Res.** |

| **E2-Res** | value_base_discontinuity_probe | 1 | ✅ | **ARTIFACT — E2's open regime CLOSES in favour of the theorem.** Decisive follow-up to E2, on the principle that a *Lipschitz* field has a finite Lyapunov exponent any error-controlled integrator must converge to (non-convergence ⇒ genuine non-smoothness). Controlled decomposition of the decisive cell: (1) under E2's own theta-only Benettin renormalization the divergence *reproduces* (λ=217→1360 as dt→DT/16) — the blow-up was dominated by the partial renorm leaving the bump-perturbation uncontrolled; (2) a proper **full-state** Benettin on the same minimal-image field kills the blow-up but stays non-convergent (λ≈0.4–2.2, noisy) — residue of the torus **minimal-image cut-locus C0-discontinuity** (local-Lipschitz max≈708), a coordinate seam, NOT an abrupt objective reordering. Swapping the seam for a C∞ periodic (von-Mises) kernel modelling the *same* PBT dynamics + full-state Benettin makes λ **converge in all 8 divergent cells** (local-Lipschitz max≈32), adaptive DOP853 flat at λ≈+0.086 over rtol 1e-6→1e-12. Decisive cell → λ*≈+0.06, recurrence **0.945** (not 0.34) — so BOTH the divergence AND the low recurrence were artifacts. Every previously-open cell = genuine finite near-zero λ (+0.02 to +0.08) with high recurrence = Case 3 (the Lorenz +0.88/0.995 band); no falsifier. Reading (a) [artifact] decisively, not (b) [genuine metanoia discontinuity]. §7.5/7.6 now holds across the FULL sweep; a genuinely discontinuous jump is ceded to Class G by design, not a smooth-flow falsifier. |

| **Hybrid** | hybrid_metric | 3 | ✅ | **NO HELP — corner is a genuine geometric limit.** Tested a drift-robust high-pass jump statistic on the square-root geometry for the Exp D structural corner. Worst corner AUC 0.65→0.61 (jump power preserved 1.00) — high-passing does not remove the confusion, so the drift/holonomy contamination is high-frequency, not a slow trend. Confirms Exp D's "structural" diagnosis; a true base-metric change (not a filtered statistic) is required. Rules out high-pass and λmin-persistence fixes. → **supplied in BaseMetric.** |

| **BaseMetric** | base_metric_corner | 3 | ✅ | **RESOLVED by a base-metric change — the corner is the square-root sphere's holonomy.** Ran the "true base-metric change" the Hybrid probe called for. Anti-developed the Exp D corner grid under three SPD base metrics, each with its own **path-wise** parallel transport: square-root (baseline, worst-corner AUC **0.65**, reproduces the confusion), **log-Euclidean** (flat → zero holonomy, worst-corner AUC **0.96**, jump power **0.90**), affine-invariant (**0.01**, worse — heavy-tailed near collapse). Log-Euclidean **resolves** the corner (≥0.85 AUC, ≥0.80 power): a flat connection means a geodesic drift accumulates zero holonomy, so no pseudo-jump, while a collapse stays an abrupt Δlogρ. Control confirms it's holonomy: base-linearizing the square-root path (discarding holonomy) lifts it identically **0.65→0.92**. Supplementary Bures-Wasserstein (base-linearized only, no path-wise transport) also helps (0.91) but is optimistic, flagged. Cost: log-Euclidean loses the exact constant-curvature structure and jump power dips 1.0→0.90, so the recommendation is a corner-specific metric switch/cross-check, not abandoning the square-root geometry. NB base-point linearization removes holonomy for ALL metrics, so the scored comparison uses proper path-wise anti-development (pre-registered validity guard tripped during a pre-run check and the path-wise fallback was used). Closes Paper 3's last synthetic drift/jump limitation. |

| **Real-EEG** | real_eeg_localization | 3 | ✅ | **SPLIT — localization confirmed still open; discrimination weaker than the appendix.** PhysioNet became reachable; ran the detectors on real eyes-open/eyes-closed EEG (15 subjects, occipital alpha). Between-record structural discrimination replicates in direction (12/15) but is **much weaker than the appendix's 20/20**: median ratio only 1.7, three subjects fail (0.77, 0.49, 0.75). The 20/20-median-12 appendix figure looks optimistic and needs reconciliation (→ **EEG-Recon**). Within-trajectory localization stays hard even for the large window (4/15 vs fragile 2/15) — the large window beats the fragile one (confirming the synthetic direction) but real sustained alpha bursts defeat it, exactly the residual limitation Exp C predicted. The 5/15 open problem is confirmed real, not a synthetic artifact. |

| **Online-CP** | online_localization_cusum | 3 | ✅ | **MATERIALLY BETTER, NOT SOLVED — a global CUSUM doubles on-line localization on real EEG.** Attacks Paper 3's headline claim (localize a transition *within* one trajectory), where the local window-mean stalled at 4/15 because sustained alpha bursts mimic a permanent shift. Two *global*, permanence-aware change-point detectors were tested directly on the same 15 real subjects (no synthetic tuning): a size-weighted geodesic change-point LR (**F-ratio, 6/15**) and a **geodesic CUSUM (8/15**, median err 2.0 s). CUSUM strictly contains the window-mean's hits and adds 4 subjects — a real, principled improvement (permanence via a whole-trajectory view), but short of the SOLVED band (≥10/15): ~half still fail (5–13 s). Honest bound: the geometry **discriminates** a structural regime and a global CUSUM **roughly doubles** on-line localization, but **on-line single-trajectory localization is improved, not solved** on this signal. Budget (1–2 tries) spent; no further detector proposed. |
| **Online-Perm** | online_localization_permanence | 3 | ✅ | **BOUND HOLDS — a permanence-explicit detector was tried and is insufficient (3/15).** Follows up Online-CP's 8/15 (short of its ≥10/15 SOLVED band) with one new global detector that scores each split by whether its post-segment matches the state the record ENDS in — penalising sustained-but-recurrent bursts that return to the pre-state. On the same 15 eegmmidb subjects (baselines reproduced: CUSUM 8/15) it localizes only **3/15**. A first run scored 1/15; diagnosis found the statistic omitted the standard change-point size weighting τ(n−τ)/n and spiked at the record boundary (the artefact the sister F-ratio documents) — a **specification defect**, fixed once, outcome-independently, recorded as a pre-registration addendum. Corrected, it reaches 3/15: still well below CUSUM. One idea, one fix, one attempt, **no tuning loop** — on-line localization of a transition that is not the dominant geometric event in its record remains OPEN, and Paper 3's 'materially better, not solved' stands. |

| **Regime-GT** | regime_groundtruth_gate | 3 | ✅ | **FAIL — Paper 3's titular claim is not merely untested but, as currently defined, NOT EXTERNALLY FALSIFIABLE.** Phase-0 gate, pre-registered before any corpus was inspected, mirroring Paper 2's `dataset_viability_gate`. Asks whether any accessible non-synthetic corpus supplies external three-way drift/dispersion/collapse ground truth. **All four candidate hosts answered 2xx/3xx — access is not the obstacle.** PhysioNet supplies *state* labels (sleep stage, eyes-open/closed), not kinematic regimes, and mapping one to the other is the 'separately contestable empirical hypothesis' §2 declines to perform. JHU Turbulence — the strongest candidate — has a genuinely external control parameter but a **binary** transition, and structural rank collapse has no turbulence counterpart, so it covers at most 2 of 3 regimes under an interpretive mapping. Financial regime labels are **model-derived** (fitted by the competitor family), failing the independence requirement by construction. Synthetic passes property 1 by construction and is excluded by the pre-registered anti-rescue guard. **The diagnosis is structural and comes from §2 itself:** the regimes are declared *operationally defined … not domain categories*, so if they are defined BY the protocol's criteria, the label IS the protocol's output and there is no independent truth-maker. Consequence: what is available is calibration of the estimator, not validation of the taxonomy. The honest routes are to re-define the regimes against an external criterion (changing the paper) or to argue the taxonomy on construct-internal grounds. The three-arm design (Euclidean / trace-normalized flat / trace-normalized curved) plus a three-state co-primary comparator is pre-registered in the gate so it cannot be chosen later. |
| **Fibre-Abl** | fibre_ablation | 3 | ✅ | **NO MATERIAL GAIN — the bundle apparatus is unearned on the paper's own worked paradigm, and it had never been exercised at all.** An external review asked which experiment isolates the fibre's contribution; a code audit found something stronger — the fibre was **unexercised**: `stats_utils.t_eff_product` (the only cross-scale-coupling helper) is never called by any experiment, no experiment builds a bundle point, and every validated result to date is a property of the trace-normalized SPD base alone. This runs the ablation the paper never ran, using Paper 3's own spec (§3.2 PAC matrix normalized by the **geometric** mean of the two block traces; §3.3 Sasaki metric = base length + covariant coupling rate). Four arms on 15 real eyes-open/closed subjects, identical windows/detectors/tolerance: base **10/15** loc, **7/15** disc (median ratio 1.26); Sasaki literal 10/15, 8/15 (1.08); Sasaki equal-weight **11/15**, **9/15** (1.16); fibre-only 2/15, 3/15 (0.87). Deltas **+1** and **+2** against a pre-registered **+3** ⇒ no material gain — and the fibre *lowers* the median discrimination ratio, diluting rather than sharpening. The verdict is readable because the **self-test passed**: on an injected PAC change with base shape held fixed, the fibre arm localized to **1.25 s** while the base missed by **14.25 s**, so the coupling estimator sees what the base cannot and the null is about redundancy, not a broken instrument. Honest consequence: on what is measured, Paper 3's validated content is the base; the fibre is genuinely discriminative for *coupling-carried* transitions but redundant on eyes-open/closed. Fibre scaling was declared in advance at two settings (literal and fibre-favourable equal-weight) so a null could not be manufactured by units. |
| **B-Real** | localization_priors_real | 3 | ✅ | **NO BENEFIT — synthetic Exp B replicates on real EEG (the object `abc_real_eeg` did not touch).** The faithful replication: it smooths the **predictability covariate** `gamma_t = conditional_residual_variance` (whose `half_window` *is* the causal smoothing bandwidth) on the real √-embedded covariance trajectory and anchors the transition at `argmax(gamma_t)` — Exp B's exact pipeline, unlike `abc_real_eeg` which smoothed the *break curve* (a different object, hence "B remains synthetic-only" until now). On 15 real eyes-open/closed subjects, sweeping `h ∈ {0..30}` windows: covariate-argmax localization is **1/15 at every bandwidth** (flat, no benefit) while peak prominence collapses monotonically **12.33 → 1.70** — smoothing only blurs the anchor, exactly the single-sample-signature mechanism synthetic B described. B's *positive* claim also holds on real data: the persistence detectors localize better than the covariate argmax (window-mean **4/15**, CUSUM **8/15** vs **1/15**). Self-test gated the run (covariate argmax found a clean no-burst seam); one run, no tuning, no threshold moved. |
| **C-Real** | cross_dataset_real | 3 | ✅ | **NOT SUPPORTED AT STRENGTH — Exp C's ratio mechanism is right-signed but weak on real data, and the design's paradigm assumption is refuted.** Supplies the real-data test `cross_dataset` said was still outstanding: it *measures* the transition/fluctuation ratio R = Δ/φ (geodesic, on the √-embedding sphere) per real recording and asks whether R predicts the tolerance-free localization error e_norm across two paradigms (eyes-open/closed alpha; Sleep-EDF W→N1). Across 22 recordings (15 alpha + 7 sleep) Spearman ρ(R,e_norm) = **−0.36** (p=0.099) — Exp C's *direction* (higher ratio → better localization) but below the pre-registered −0.40 bar and n.s. Decisively, the ordering the design assumed is **false**: measured, sleep-onset W→N1 is **not** higher-ratio than alpha (median R 0.68 vs 0.69) and localizes no better (median e_norm 0.156 vs 0.078) — so (i) the design did not span the ratio axis (W→N1 is a gradual, low-ratio transition on this geometry) and (ii) Exp C's synthetic "robust across paradigm strength" (15/15 at every ratio) does **not** transfer — the same synthetic-overstates-real pattern documented for **A** (2/15→15/15 becoming 4/16→7/16). Self-test gated the run (ordered known synthetic ratios R=20.4 vs 3.5 correctly); no threshold moved. The fair test — a genuinely high-ratio real transition (N2↔REM) as the high end — is **registered as a follow-up**, not run post-hoc to chase the bar. |
| **EEG-Recon** | eeg_reconciliation | 3 | ✅ | **SIGNIFICANCE REPRODUCED, MAGNITUDE NOT — the appendix's ≈12 is optimistic.** Isolated the cause of the 1.7-vs-12 gap on the same 15 cached subjects. The difference was the within-state denominator: `real_eeg_localization` used a temporal-half split (which counts slow within-state alpha drift), the appendix names a **within-state permutation null** (random interleaved splits — drift cancels). Switching *only* that estimator lifts the median ratio 1.28 → 3.33 (14/15 > 1) and makes it significant in 14/15 (median p≈0.0025, near the appendix's single-subject 5×10⁻⁴; Wilcoxon vs unity p≈6×10⁻⁵) — so the appendix's **direction and significance replicate**. But the magnitude does **not**: median 3.33 ≪ the appendix's ≈12, and a secondary seg(20–58 s)×window(0.5–2 s) sweep never exceeds median 5.4. Honest reconciled figure: **~3–5** (permutation) or **~1.3** (temporal-half), not 12. The qualitative claim (real, significant, replicated structural discrimination) stands; the specific magnitude figure should be corrected downward. |

| **Escape** | escape_endogeneity | 1 | ✅ | **E1 REFUTED; E2 INCONCLUSIVE — and the structure of the argument is the finding.** Targets the clause carrying §7.5's escape horn, separating two claims the prose runs together: **E1** escape requires an external occasion, **E2** escape forfeits persistence. **E1 is false**, and was declared false in the pre-registration *before* the run so it could not be sold as a discovery: `finite_time_blowup` (θ̇=θ‖θ‖) is autonomous, has no external term, and leaves every compact set in finite time. **E2**: the escaping candidates are open (`escaping_chaos` angular λ=+0.33, `freezing_escape` +0.20) but **remain RECURRENT** in the identity observable (0.98, 0.96). ⚠️ An earlier version of this row claimed they achieved non-recurrence too, and that claim reached Paper 1 before being caught: crossing the log-radius *measurement* bound was treated as a finite-time blow-up, overriding a measured 0.98 to 'non-recurrent'. **Withdrawn and reversed in Paper 1.** But the persistence failure **cannot be attributed to escape**: the bounded control fails it too (P_f=0.0011), because the only openness these candidates have is chaos and chaos decorrelates regardless. Attribution check added AFTER the run and recorded as such; it downgrades the conclusion rather than changing the rule. Deeper issue surfaced: 'openness' here is positive-Lyapunov (§7.5's sense) while Class G §8.3 demands the opposite signature (|λ∥|≈0, incommensurable, correlation-retaining) — the paper uses both senses and they come apart; the deciding candidate (aperiodic + persistent + non-chaotic) is registered as a follow-up rather than added post hoc. **Four instrument defects found before reading results**, two of which flattered the paper (full-state Lyapunov positive under pure expansion; finite-time blow-up scored recurrent via an estimator NaN), plus a design flaw that made openness topologically impossible for the escaping candidates (autonomous flow on S² — Poincaré–Bendixson). All fixed; a 7-check self-test now gates the run. |
| **Escape-Dec** | escape_persistence_decider | 1 | ✅ | **E2 SURVIVES, ATTRIBUTABLY — but §7.5 closes the escape horn with the WRONG argument.** The follow-up `escape_endogeneity` registered. It asks the question in **Class G's own sense of openness** (|λ|≈0, incommensurable, correlation-RETAINING) rather than §7.5's (positive entropy, correlation-destroying) — the two senses the paper uses interchangeably are near-opposites, and Class G needs the second. Control passes openness+persistence together (P_f=0.157, p=0.005), so failures ARE attributable — the check `escape_endogeneity` lacked. **Persistence SURVIVES escape**: quasi-periodic escapers hit P_f=0.17–0.21 (bar 0.05) while running to log r=25, so calling unbounded drift 'a more extreme Class M' is inaccurate. What fails universally is **non-recurrence** (0.81–1.00): escape happens in the RADIUS, the contract reads the DIRECTION, and the direction lives on a compact quotient where Poincaré applies unchanged. The horn closes by the *same* argument as the bounded cell — stronger and more general than the paper's. (**Cardinal** follow-up removes even the scale-free caveat: see **Escape-Card**.) `commensurable_escape` correctly fails aperiodicity (isolates incommensurability as load-bearing); `chaotic_escape` reproduces the sister experiment exactly. Two sanity adjustments recorded, no threshold moved. |
| **Escape-Card** | escape_cardinal_contract | 1 | ✅ | **THE HORN CLOSES FOR CARDINAL CONTRACTS TOO, BY THE SAME (RECURRENCE) ARGUMENT — so §7.5's price is refunded.** `Escape-Dec` closed the scale-free case and flagged that the exclusion might depend on the contract being scale-free. Tested directly: the escaping quasi-periodic witness under a FAMILY of cardinal (magnitude-reading) contracts — bounded `saturating`, linear `log_scaled`, exponential `raw_coord` — each reducing to the scale-free observable for a bounded system so the control passes and the verdict is attributable. **Every cardinal contract fails NON-RECURRENCE** (rec 0.81–0.93): no magnitude weighting removes the return the oscillating direction on the quotient supplies. Persistence collapses ONLY under the exponential `raw_coord` (P_f=0.0006) while `saturating` (0.21) and `log_scaled` (0.15) retain it. ⚠️ **The verdict's reading of that collapse as a "numerical artefact of an e²⁵ observable" has since been WITHDRAWN as an over-inference** (external criticism; recorded in METHODOLOGY.md). Fast-growing contracts are simultaneously the only ones that could break recurrence and the only ones that overflow the estimator, so the exclusion is systematic and correlated with the property under test — the same defect Paper 2 §8.1 identifies in someone else's confound. Corrected reading: exponential contracts are **not testable by this instrument**, not confirmed artefactual. The raw result is unchanged; the inference drawn from it is narrower. Consequence: the horn closes **for contracts factoring through a bounded reading** by recurrence-on-the-quotient (scale-free, plus bounded/linear cardinal); §7.5's 'more extreme Class M' is vindicated by nothing, and the scale-free dependency `Escape-Dec` conceded is withdrawn. The closure is **conditional, not general** — compactness moved from the state space to the observable — and exhaustivity over admissible contracts is a conjecture: the deciding cell (unbounded reading + open direction) is empty here and unreachable by the instrument. Self-test 7/7; no threshold moved. |
| **ClassG** | class_g_coherence | 1 | ✅ | **NON-VACUOUS AND DISCRIMINATING — §8.3's ten-condition conjunction is satisfiable.** The existential-risk check: Class G is Paper 1's whole positive residue, defined as a conjunction of **ten** conditions that had never been tested for satisfiability (the Paper 2 §15.2 toy tests four diagnostics and never touches conditions 1, 3, 8, 9, 10). An explicit system (double-well regime coordinate + contracted transverse deviation + two incommensurable protected phases + OU agency + gated landscape-lowering intervention) passes **all ten** against pre-registered thresholds: barrier 0.250→0.107 (MFPT 386→187), Var(agency) 0.50, D_id 0.020 vs **measured** D_c 0.100, λ⊥ −1.01, λ∥ +0.000, P_f 0.22, exact-period error 4e−2 ≫ 1e−6, incommensurable, rank-1 gate residual 1.7e−16 with a gate that closes, endogenous-projection residual 0.60. Also **discriminates**: all 5 near-misses from §8.3's own filter table excluded, each failing a predicted condition (damping 2/5/6/7; ungated 3/9; endogenous 1/10; coercion 2/9; commensurable 7/8). Two instrument defects found+fixed *before* reading the result (integer-lag search can't see a period falling between samples; coarse refinement left a 1e−5 floor above the 1e−6 bar, misreading a pure sine as aperiodic) — caught by a detector self-test now gating every run; thresholds NOT loosened, numerics tightened, and the fixes made discrimination stronger. Scope: logical consistency only — says nothing about instantiation. |
| **Bench** | baseline_benchmark | 3 | ✅ | **MIXED, AND REPORTED ON THREE AXES RATHER THAN THE FLATTERING ONE.** The protocol had never been compared to anything. Against BOCPD, `ruptures` (PELT/BinSeg/Window), a 2-state Gaussian HMM and sliding-window k-means, each given the same cov/log-cov/power features — 19 method-feature combinations. **(1) Localisation on the real task: WIN by the pre-registered rule**, geodesic CUSUM 10/15 vs best baseline rupturesWindow[logcov] 8/15 — but a two-recording margin on fifteen is well inside binomial noise and is read as "not worse, plausibly better". **(2) Detection: the benchmark made it the WORST method compared** (AUC 0.227 vs baselines 0.67–0.88), an anti-correlated result later traced to the confidence statistic and repaired in **DetRepair**; the 0.227 is retained here unaltered as the record of what peak-to-median actually scored. **(3) Structure-vs-power scenarios**: on S1 (structural change, total power constant) the geometry scores 1.00 against the best power-feature baseline's 0.75 — real but **below** the +0.30 margin fixed in advance, so *not established at the pre-registered level*; on the mirror S2 (pure power change) it scores 0.55 against 1.00, losing exactly as its construction requires. A centre-bias leak found after the first run (fallbacks returning the record midpoint, which was the true answer) reversed the localisation result from LOSS to WIN; because the correction favoured this project, its provenance is recorded in full and the fix is justified by an outcome-independent impossibility. |
| **DetRepair** | detection_statistic_repair | 3 | ✅ | **REPAIRED — the sub-chance detection AUC was statistic design, not geometry.** The benchmark scored the CUSUM's confidence as peak/median, which rewards the null by construction: a real change point makes a *tent* (median ~ peak, modest ratio) while a driftless random walk sits near zero (small median, large ratio). H1 confirmed **before** scoring repairs: real curves more tent-like (R² 0.826 vs 0.704) AND nulls higher on peak/median (1.80 vs 1.53). Four pre-registered repairs against a fixed bar (AUC ≥ 0.70, lose ≤ 1 localisation hit); the **cheapest passes**: scale-normalising the peak by the segment's own increment scale and length gives **AUC 0.227 → 0.813** with localisation unchanged at **10/15**. Surrogate calibration (0.39), tent-shape R² (0.65), and a window-mean gate (0.58) fail the bar and are reported. At 0.813 the detector is **4th of 19** (itself against six baseline algorithms × three feature sets) — competitive, not superior (best baseline 0.880). Retreat clause not triggered; Paper 3 now specifies the detection statistic as part of the method. |
| **DetRepair-HO** | detection_repair_heldout | 1 | ✅ | **THE REPAIR GENERALISES OFF THE DATA IT WAS CHOSEN ON.** DetRepair selected among four candidate statistics on the *same* 15 Sleep-EDF records that exposed the problem — no held-out test. The committed statistic was therefore applied **unchanged** to a paradigm not used to select it (eegmmidb R01 eyes-open vs R02 eyes-closed, occipito-parietal alpha, 16 subjects). Scale-normalised **AUC 0.824**, clearing the pre-registered 0.70 bar; the old peak-to-median statistic scores **0.434** on the *identical* segments — sub-chance again, so the **failure** mechanism generalises as well as the fix, which is what the mechanistic diagnosis predicted and what single-dataset selection could not establish. Nothing re-tuned. The secondary localisation count (9/16) is **deliberately not quoted**: this design splices two runs so the seam always falls at the segment centre, flattering any localiser with a central prior; it is not the appendix's eyes-open/closed protocol, whose negative localisation finding stands unrevised. Detection only. |
| **ClassG-Ind** | class_g_independence | 1 | ✅ | **EFFECTIVE DIMENSIONALITY ≈ 6, NOT 10 — declared rather than left for a reviewer.** Prompted by an audit finding that 3/5 near-misses in ClassG fail MORE conditions than the filter table predicts (damping [2,5]→[2,5,6,7]; ungated [9]→[3,9]; endogenous [10]→[1,10]). Perturbing surgically for each condition in turn: **6 of 10 break in isolation** (1,2,3,4,6,9); four never do (5,7,8,10). Measured entailments 5⇒6, 5⇒7, 8⇒7, 10⇒1. Two are **logical**, not artefacts: an incommensurable drive is aperiodic by definition (8⇒7 admits no separating construction), and a collapsed phase is a constant, hence without correlation power and trivially periodic (5⇒6,7). Satisfiability and discrimination untouched; what changes is how much weight the count of ten can bear. Two default-inert parameters were added to the witness for surgical targeting, and ClassG was re-run to confirm its result is unchanged. **The metric is now stated explicitly**, after an audit found the run reported *column* clusters (conditions sharing a breaker-set) while "identical failure signature" naturally reads as *rows*: "effective dimensionality 6" counts conditions breakable **in isolation** — the conservative reading, least flattering to the paper. Counting distinct failure patterns instead gives **9 rows** (only 7 and 8 coincide, and inseparably) or **10 columns**; a reader computing it differently lands between 6 and 10, and 6 is defended as the lower bound. NB pre-registration written AFTER the run (a measurement with no tunable threshold) — recorded in the file and in METHODOLOGY.md. |
| **ABC-Real** | abc_real_eeg | 3 | ✅ | **A REPLICATES IN DIRECTION, NOT IN MAGNITUDE.** Runs the scale comparison A1 skipped, on A1's own 16 Sleep-EDF recordings with its loading code imported unchanged. Window size does the work: large-40 **7/16** vs best short **4/16** (clears the pre-registered 3-hit margin), and the multiscale bank returns **6/16** — within a hit, so it adds nothing, exactly as A claimed. **But synthetic A moved 2/15→15/15 while real data moves 4/16→7/16**, under half; the synthetic perfect score implied a solved problem that real EEG does not support — the same pattern as the appendix's 12× corrected to 3.3×. B's analogue (smoothing the break curve) gives 7/16 vs 7/16, i.e. no help, consistent with B — but this is an **analogue**, not a replication: B smooths the predictability covariate inside the jump-anchoring pipeline, a different object, so **B remains synthetic-only**. **C is not addressed.** |
| **Fase 0** | dataset_viability_gate | 2 | ✅ | **PASS — I-CARE is the only public dataset with the three CBRA properties.** Pre-registered dataset-viability gate (concurrent cardiac+neural; I+/I− contrast; MR-length). I-CARE post-arrest cohort: EEG+ECG, CPC recovery-vs-non-recovery = §14.1's "matched non-recovering control", hours-to-days records, open. VitalDB pure-I+; sleep banks all I+; ICU (MIMIC) no concurrent EEG; seizure banks no cardiac + no I-status contrast. PASS narrow, two empirical gates for Trilha B (ECG coverage; audited matching). |
| **A1** | sleep_stage_localization | 3 | ✅ | **GENERALIZES on a 2nd real paradigm (Sleep-EDF).** Structural N2-vs-REM discrimination **14/15** under permutation null (median ratio 2.79); within-trajectory **sleep-onset localization CUSUM 10/15** (F-ratio 6/15, window-mean 6/15) vs 4/15 on eyes-open/closed → the appendix's 5/15 limit was the **spontaneous-alpha paradigm, not the method**. Honest: 15 recordings from 8 subjects (both nights); all localized transitions are W→N1 (sleep-onset); 2 misses near-miss at 31s (±30s not relaxed). |
| **A2** | sleep_structure_power_dissociation | 3 | ✅ | **QUALIFIED replication — and the qualification is the finding.** Structure fires (N2-vs-REM 14/15). Pre-registered natural-power arm (within-N2 hi/lo power) is NOT silent (median 1.89) — post-hoc pure-amplitude control (random N2 halves) IS silent (median 1.00, 12/15), so the geometry is power-blind by construction and the natural-power leak is genuine structure. The appendix's clean "silent on a NATURAL power transition" is **paradigm-specific** (holds when the power change preserves structure; fails when power/structure are confounded, as in sleep). |
| **A3** | log_euclidean_real_eeg | 3 | ✅ | **log-Euclidean VIABLE on real EEG; weak-collapse cost 0.85→0.22.** Closes BaseMetric's synthetic loop. Real Sleep-EDF: discrimination sqrt 14/15 vs log-Euclidean **15/15** (higher ratios), localization 10/15 vs 10/15 — the flat metric doesn't break real detection (slightly better). Cost now on record: corner AUC 0.65→0.96 but weak-collapse (0.7) jump power **0.85→0.22** (recovers on stronger collapses). Recommendation: log-Euclidean corner-specific cross-check, **sqrt primary**. |
| **B1** | cbra_boundary_residual | 2 | ✅ | **FAILS → Trilha B STOPS (pre-registered).** Gating step on I-CARE. Part A: 45/60 patients have ECG+EEG (75%). Part B: RR-tachogram interoceptive boundary → time-reversal asymmetry vs 200 IAAFT surrogates — only **6/21 (29%)** show structure beyond a linear-Gaussian null, below the 60% bar. So the boundary residual is NOT estimable where the I+/I− contrast exists → B2/B3 NOT run. Honest: the fixed-threshold R-peak detector was inadequate and replaced with adaptive Pan-Tompkins *before* reading (made the negative stronger, 29% vs 42% — defect fix); 3/6 "structured" have arrhythmia-scale \|T_rev\|>0.5 (over-count); post-arrest ECG confounded by sedation/TTM/pressors. → Paper 2 §14.2 gets the data-availability estimability condition. |

## Recommended execution order
This is the *original core* order; the follow-up rows above (E2, E2-Res, Hybrid,
BaseMetric, Real-EEG, Online-CP, EEG-Recon, Fase 0, A1, A2, A3, B1) were added later
as each core result raised a sharper question, and each names its own antecedent.

**D → A → B → C**  (Phase 1, Paper 3)
**G → F → E**  (Phase 2, Paper 1)
**H → I → J**  (Phase 3, Paper 2)

## Notes
- Each experiment writes machine-readable results to `_results/<experiment>/`.
- Verdicts are always issued against the `PRE-REGISTRATION.md` criteria written
  before the run — never adjusted after seeing results.
