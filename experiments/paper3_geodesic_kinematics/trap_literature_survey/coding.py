# survey coding, appended batch by batch: rank -> dict
CODES = {}
EXCL = {}
def inc(rank, pmc, **kw): CODES[rank] = dict(pmcid=pmc, **kw)
def exc(rank, pmc, why): EXCL[rank] = dict(pmcid=pmc, why=why)

inc(1,"PMC11991455", task="MI and P300 BCI benchmark (MOABB, 20 databases)",
    centre=("N/A",""),
    dependence=("Yes","'estimated via a within-session stratified 5-fold cross-validation' (trials of a session assigned to folds at random)"),
    pseudo=("No","paired tests across subjects within each database"),
    ocular=("Yes","'all available trials were used without any artifact rejection' (cue-based MI/P300; frontal channels in the covariance)"),
    recording=("No","MI/P300 classes interleaved within runs"))
exc(2,"PMC12521986","fNIRS, not EEG")
inc(3,"PMC12431285", task="same-limb motor imagery (BCI Competition IV-2a, custom NeuroSCP)",
    centre=("N/A",""),
    dependence=("Yes","'a stratified five-fold cross-validation was applied' to trials within subject"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("Yes","'we did not use artifact removal and rejection methods' (cue-based MI)"),
    recording=("No","classes interleaved within runs / segmented phases within trials"))
inc(4,"PMC11963017", task="GREEN: age, dementia, eyes-closed vs eyes-open prediction",
    centre=("N/A",""),
    dependence=("Unclear","'100 Monte-Carlo splits, 20 % test set size'; whether splits are by subject or by window is not stated in the extracted methods"),
    pseudo=("Unclear","not determinable from methods text"),
    ocular=("No","'For the eyes-open versus eyes-closed classification task, we additionally included an ICA ... step to remove ocular artifacts that would otherwise be trivially predictive'"),
    recording=("Unclear","origin of EO/EC labels (separate recordings or segments) not stated in extracted text"))
inc(5,"PMC12319850", task="Deep Riemannian networks (SPDNet) for MI/ERP decoding, five public datasets",
    centre=("N/A",""),
    dependence=("No","'For final evaluation we used the default train/test splits of all datasets' (session-wise splits)"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("Yes","no ocular artefact handling reported; frontal channels in MI montages"),
    recording=("No","classes interleaved within runs"))
inc(6,"PMC11351530", task="motor imagery, multiple tangent-space projections (BCI Competition IV 2a/2b)",
    centre=("N/A",""),
    dependence=("Unclear","train/test protocol not described in the extracted methods"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("Yes","'Our proposed method mitigates noise from muscle movements, eye blinks ... by focusing on channel relationships through covariance matrices' (no removal reported)"),
    recording=("No","MI classes interleaved within runs"))
inc(7,"PMC12237276", task="cross-subject motor imagery, Riemannian alignment + tangent space (three public datasets)",
    centre=("N/A",""),
    dependence=("No","'EEG recordings from a subject's first session were split into training and validation ... evaluated on the test session data'"),
    pseudo=("No","'In the Cross-Subject scenario, the network was trained on data from all other subjects and tested on the target subject'"),
    ocular=("Unclear","ICA is described as a method; whether it was applied to these data is not stated in the extracted text"),
    recording=("No","MI classes interleaved within runs"))
exc(8,"PMC12523035","fNIRS, not EEG")
inc(9,"PMC11169813", task="Riemannian transfer learning (log-Euclidean), BCI Competition III IIIa and IV 2a",
    centre=("N/A",""),
    dependence=("No","cross-session / cross-subject evaluation; five-fold CV only for hyper-parameters 'on the training fold'"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("Yes","no ocular artefact handling reported (0 mentions); cue-based MI"),
    recording=("No","MI classes interleaved within runs"))
inc(10,"PMC12368069", task="lower-limb motor imagery in knee-pain patients, Riemannian distance classifier",
    centre=("N/A",""),
    dependence=("Unclear","'for that subject and cross-validation split' — fold construction not described"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("Yes","EOG recorded 'to monitor artifacts', but preprocessing reported is a notch filter only; frontal channels in the covariance"),
    recording=("No","MI classes interleaved within runs"))
inc(11,"PMC10696092", task="path-signature SPD matrices with Riemannian tangent-space classifier, MI-BCI datasets",
    centre=("N/A",""),
    dependence=("Yes","'Cross-validation (Ten-fold for intra-subject ...)' over trials within subject"),
    pseudo=("No","'leave-one-out for inter-subject'"),
    ocular=("Unclear","'There were also 3 EOG channels, but they were not used'; trials with artifacts 'were marked by the dataset providers' — rejection not stated"),
    recording=("No","MI classes interleaved within runs"))
exc(12,"PMC11853533","no EEG covariance/SPD features in the methods (hybrid dry-EEG + pupillary-reflex BCI)")
inc(13,"PMC12007539", task="harmonizing M/EEG datasets with covariance-based alignment for age prediction",
    centre=("N/A",""),
    dependence=("Unclear","split construction not determinable from the extracted text"),
    pseudo=("N/A","outcome is a subject property (age); same-subject cross-task conditions are an explicit design factor"),
    ocular=("N/A","prediction target (age) is not an eye-movement contrast; artefacts handled by 'local autoreject'"),
    recording=("N/A","outcome is not a within-subject state contrast"))
exc(14,"PMC11995804","MEG, not EEG")
exc(15,"PMC12238400","Riemannian/covariance methods appear only in the related-work discussion, not in the study's own pipeline")
exc(16,"PMC12782479","full text not retrievable from Europe PMC (abstract only)")
inc(17,"PMC12297340", task="PhysioNet motor movement/imagery, classical and deep classifiers with Riemannian preprocessing",
    centre=("N/A",""),
    dependence=("Unclear","train/test construction not described ('average performance metrics computed on the test set')"),
    pseudo=("Unclear","not determinable"),
    ocular=("Unclear","generic 'artifact removal' in the pipeline; ocular handling not specified"),
    recording=("No","T1/T2 classes interleaved within runs"))
inc(18,"PMC10823003", task="emotion recognition on SEED, functional-connectivity features on the SPD manifold",
    centre=("N/A",""),
    dependence=("Unclear","'subject dependent cross-validation strategy' with non-overlapping 1-s windows; fold construction not described"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("Unclear","'The artifact was also been removed' — ocular handling not specified; film stimuli"),
    recording=("No","emotions elicited by clips within the same session"))
inc(19,"PMC12578254", task="CVAE for neurofeedback; impaired vs healthy EEG, with a Riemannian SVM (covariance) baseline",
    centre=("N/A",""),
    dependence=("Yes","per-window feature matrices with 'train/test splits' over windows; 93% 'on the unseen test split'"),
    pseudo=("Unclear","not determinable"),
    ocular=("Unclear","'artifact-free epochs' — criterion not specified"),
    recording=("Yes","classes drawn from two different datasets: 'Our study involved using two public OpenNeuro datasets' (impaired vs healthy), with no same-condition cross-dataset control"))
inc(20,"PMC10973000", task="DCCA-enhanced Riemannian decoder for motor imagery, offline and online",
    centre=("N/A",""),
    dependence=("No","'maintaining the temporal causality of the incoming epochs' in the evaluation schemes"),
    pseudo=("N/A","per-subject evaluation"),
    ocular=("No","'Incoming trials were rejected from decoding if blinks or large, sudden eye movements were detected in the EOG'"),
    recording=("No","MI classes interleaved within runs"))
inc(21,"PMC12563875", task="P300 spatiotemporal covariance patterns and transformer-CNN for PTSD vs controls",
    centre=("N/A",""),
    dependence=("No","'We employed a subject-level 4-fold cross-validation strategy'"),
    pseudo=("No","subject-level folds"),
    ocular=("Unclear","Fp1/Fp2 included; ocular handling not found in the extracted text; group contrast"),
    recording=("N/A","between-subject group contrast"))
inc(22,"PMC12494903", task="hierarchical deep learning for four-class motor imagery with CSP/Riemannian covariance features",
    centre=("N/A",""),
    dependence=("No","'leave-two-subjects-out cross-validation scheme'"),
    pseudo=("No","subjects held out"),
    ocular=("Unclear","channels flagged by amplitude/kurtosis thresholds; ocular handling not specified; visual cues"),
    recording=("No","MI classes interleaved within runs"))
inc(23,"PMC11763639", task="unsupervised EEG microstate segmentation using spatial patterns and Riemannian distance",
    centre=("N/A",""),
    dependence=("N/A","unsupervised clustering; no CV accuracy or permutation p-value reported"),
    pseudo=("N/A",""),
    ocular=("No","'two components associated with eyeblinks or horizontal eye movements were removed' (ICA)"),
    recording=("N/A","segmentation, not a between-condition contrast"))
inc(24,"PMC9139384", task="kernel domain adaptation on the SPD manifold for cross-subject motor imagery",
    centre=("N/A",""),
    dependence=("Unclear","target-subject train/test construction not determinable from the extracted text"),
    pseudo=("N/A","per-target-subject evaluation"),
    ocular=("No","'we removed the trials containing an artifact label'"),
    recording=("No","MI classes interleaved within runs"))
inc(25,"PMC12216635", task="correlation-matrix (SPD) geometry toolbox; EEG two-sample hypothesis test on PhysioNet motor movement/imagery",
    centre=("N/A",""),
    dependence=("Unclear","permutation test 'permuting class labels'; composition of the two sets of 45 correlation matrices not determinable"),
    pseudo=("Unclear","not determinable"),
    ocular=("Unclear","no ocular handling reported for the EEG data"),
    recording=("Unclear","what defines the two sets (runs, tasks or groups) not determinable"))
exc(26,"PMC12358810","fMRI only")
inc(27,"PMC12259573", task="impact of cross-validation choices on passive-BCI metrics (three n-back datasets; Riemannian MDM and FBCSP-LDA)",
    centre=("N/A",""),
    dependence=("No","compares block-respecting and non-respecting CV explicitly; 'RMDM classifiers may differ by up to 12.7%'"),
    pseudo=("N/A","per-participant evaluation"),
    ocular=("Yes","no ocular removal: 'removing artifacts via independent component analysis (ICA) would likely improve the accuracy metrics'; visual n-back task"),
    recording=("No","block structure of conditions is the object of study and is respected in the recommended scheme"))
